beautifulsoup4
lxml
fastapi
uvicorn[standard]
jinja2
pydantic
python-multipart
cryptography
# ao3downloader - using local repo (added to sys.path in code)
# Note: PyPI package requires Python <3.13, but local repo works with any version
